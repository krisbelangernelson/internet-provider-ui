import { type ReactElement } from 'react'
import Person from '@/assets/icons/person-circle.svg'

const MyAccount = (): ReactElement => (
  <>
    <Person style={{ fontSize: '2rem', color: 'lightblue' }} />
  </>
)

export default MyAccount
